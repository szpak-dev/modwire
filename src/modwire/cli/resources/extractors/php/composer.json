{
  "private": true,
  "type": "project",
  "require": {
    "nikic/php-parser": "^5.6"
  },
  "scripts": {
    "build": "php -d phar.readonly=0 build.php",
    "check": "php -l script.src.php && php -l build.php"
  },
  "config": {
    "sort-packages": true
  }
}
